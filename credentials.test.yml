# Production credentials for channels

# This file contains the credentials for the voice & chat platforms
# which your bot is using.
# https://rasa.com/docs/rasa/messaging-and-voice-channels

abot.channel.rest_fixed.RestInputFixed:

# Telegram (custom) channel
abot.channel.telegram.TelegramInput:
  access_token: !env_var ${TELEGRAM_BOT_TOKEN}
