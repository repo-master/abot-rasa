
// From AggregationMethod enum
@[aggregation]
    ~[recent] = recent
    ~[average] = average
    median
    ~[minimum] = minimum
    ~[maximum] = maximum
    ~[stddev] = stddev
    ~[count] = count
    ~[compliance] = compliance
    ~[quantile] = quantile
    ~[summary] = summary

@[number]
    25
    22.5
    99.4
    50
    32
    28

|aggregation_aliases.chatette
